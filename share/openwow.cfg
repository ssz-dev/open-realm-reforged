// OpenWoW gameplay defaults.
// User overrides are written to share/openwow-config.cfg.

bind MOUSE1 "+wowselect"
bind MOUSE2 "+look"
bind w "+forward"
bind s "+back"
bind a "+moveleft"
bind d "+moveright"
bind ESCAPE "quit"

seta wow_mouse_speed "0.18"
seta wow_zoom_speed "1.0"
seta wow_save "share/openwow-progress.sav"
